import { CSSProperties } from 'react';

export const styles = {
    container: {
        border: '1px solid rgba(255, 255, 255, 0.1)',
        padding: '1rem',
        borderRadius: '8px',
        background: 'rgba(0, 0, 0, 0.2)',
    } as CSSProperties,
};